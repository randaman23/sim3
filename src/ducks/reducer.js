const initialState = {
  username: {},
  id: {},
  profilepic: {}
};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    

    default:
      return state;
  }
}
